# Quick Overview
- The `fabric-test.sh` shell script is only for testing. It's from the official Fabric Samples repository. The Fabric Samples are required for the binaries, templates, and shell scripts.
- The `linaw-setup.sh` shell script will setup our thesis stuff. This will download all the binaries, dependencies, and packages of our project into the user's host machine.
